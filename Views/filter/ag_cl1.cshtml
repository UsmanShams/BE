@model IEnumerable<be.Models.Customerledger>

@{
    ViewData["Title"] = "cl1";
    Layout = "~/Views/Shared/home.cshtml";
}
<br>>
<a class="btn btn-primary btn-rounded btn-fw" asp-controller="filter" asp-action="ag_cl" asp-route-id="@ViewBag.iid">Aging</a>

<div class="col-lg-6 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h2 class="card-title">Customer Ledger</h2>
            <h3 style="color:black;">@ViewBag.id</h3>
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>
                            Order #
                        </th>
                        <th>
                            Date
                        </th>
                        <th>
                            Time
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Description)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Qty)
                        </th>
                        <th>
                            Credit
                        </th>
                        <th>
                            Debit
                        </th>
                        <th>
                            Balance
                        </th>
                    </tr>
                    </tr>
                </thead>
                <tbody>
                    @{
                        var bal = 0;
                    }
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Orderid)
                            </td>
                            <td>
                                @item.day-@item.month-@item.year
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.time)
                            </td>@{
                                if (item.type == "Drum" || item.type == "Druml" || item.type == "Pail")
                                {
                                    BentContext db = new BentContext();
                                    var x = db.Products.Where(x => x.PId == Convert.ToInt32(item.Description)).First();
                                    <td>@x.PName</td>
                                }
                                else
                                {
                                    <td>@item.Description</td>
                                }
                            }
                            <td>
                                @Html.DisplayFor(modelItem => item.Qty)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ClIn)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ClOut)
                            </td>
                            @{
                                if (item.ClIn == 0)
                                {
                                    bal = bal - Convert.ToInt32(item.ClOut);
                                }
                                else if (item.ClOut == 0)
                                {
                                    bal = bal + Convert.ToInt32(item.ClIn);
                                }
                            }
                            <td>@bal</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>